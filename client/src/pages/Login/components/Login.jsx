import AuthorizationForm from "../../../components/AuthorizationForm";

const Login = ({
                   children,
               }) => {
    return(
        <AuthorizationForm />
    );
};

export default Login;